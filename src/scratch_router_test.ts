import { BrowserRouter, Routes, Route, Link, useNavigate, useParams, useLocation } from 'react-router';

console.log("BrowserRouter is:", typeof BrowserRouter);
console.log("Routes is:", typeof Routes);
console.log("Route is:", typeof Route);
console.log("Link is:", typeof Link);
console.log("useNavigate is:", typeof useNavigate);
console.log("useParams is:", typeof useParams);
console.log("useLocation is:", typeof useLocation);
